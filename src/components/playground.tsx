"use client";
import React from "react";
import Editor from "@/components/editor";

type PlaygroundProps = {
  handleSave: () => Promise<void>;
};
function Playground({ handleSave }: PlaygroundProps) {
  return;
}

export default Playground;
